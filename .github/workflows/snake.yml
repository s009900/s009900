name: Generate two contribution snakes

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Generate default snake
        uses: Platane/snk@v3
        with:
          github_user_name: ${{ github.repository_owner }}
          outputs: |
            dist/default-snake.svg

      - name: Generate pink snake
        uses: Platane/snk@v3
        with:
          github_user_name: ${{ github.repository_owner }}
          outputs: |
            dist/pink-snake.svg
          config: .github/snk-pink.yaml

      - name: Commit and push generated snakes
        uses: EndBug/add-and-commit@v9
        with:
          add: 'dist/*.svg'
          message: 'chore: generate and update contribution snakes [automated]'
          default_author: github_actions
